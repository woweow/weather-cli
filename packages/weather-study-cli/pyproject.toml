[project]
name = "weather-study-cli"
version = "0.1.0"
description = "CLI for validating local raw weather-market study captures"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
weather-study = "weather_study_cli.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
