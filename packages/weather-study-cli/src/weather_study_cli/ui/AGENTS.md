Purpose: HTML/CSS/JS rendering for the local study visualization.

Rules:

- Own the exported accuracy dashboard document and browser-side interactions.
- Keep SQLite access and aggregation logic out of this layer.
- Favor self-contained output that works as a standalone local HTML file.
