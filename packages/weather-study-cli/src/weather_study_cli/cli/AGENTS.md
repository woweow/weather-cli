Purpose: the `weather-study` command-line surface.

Rules:

- Own argparse, help text, and user-facing summary output.
- Keep schema validation and file loading in the application layer.
- Document that local raw files share one code path for bundled mock data and future S3 downloads.
