[project]
name = "kalshi-weather-cli"
version = "0.1.0"
description = "CLI for Kalshi daily high-temperature market snapshots"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
kalshi-weather = "kalshi_weather_cli.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
