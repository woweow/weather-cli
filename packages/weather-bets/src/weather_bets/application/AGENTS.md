Purpose: journal use-cases on top of the persistence layer.

Rules:

- Own record/list/show/settle flows.
- Own the clean-slate `init --reset` behavior exposed by the CLI.
- Return provider ids, settlement metadata, and simulator fields in machine-friendly form.
- Return machine-friendly data structures for both the CLI and dashboard server.
