Purpose: the canonical decision snapshot contract and journal-level error semantics.

Rules:

- Own validation and normalization of dashboard snapshots.
- Keep this layer persistence-agnostic and UI-agnostic.
