[project]
name = "weather-bets"
version = "0.1.0"
description = "Local SQLite journal for weather bet decision sessions and settlement"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
weather-bets = "weather_bets.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
