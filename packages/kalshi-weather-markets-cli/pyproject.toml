[project]
name = "kalshi-weather-markets-cli"
version = "0.1.0"
description = "CLI for Kalshi daily high-temperature market ladders"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
kalshi-weather-markets = "kalshi_weather_markets_cli.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
