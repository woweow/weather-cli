[project]
name = "weather-dashboard-cli"
version = "0.1.0"
description = "CLI for serving and exporting the local weather decision dashboard"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["weather-bets"]

[project.scripts]
weather-dashboard = "weather_dashboard_cli.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
