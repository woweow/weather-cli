Purpose: HTML/CSS/JS presentation for the local dashboard.

Rules:

- Own rendering and browser interaction behavior.
- Own yes/no toggles, side-specific stake inputs, and lightweight in-browser simulator previews.
- Do not add SQLite logic or server routing here.
