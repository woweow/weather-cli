[project]
name = "weather-cli"
version = "0.1.0"
description = "CLI for yesterday, today, and +/-24h weather.gov weather data"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
weather = "weather_cli.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
